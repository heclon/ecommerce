import React, { ReactElement } from 'react'
import { Loader } from './Loader'

export default {
  title: 'Components/Loader',
  component: Loader,
}

export const Example = (): ReactElement => <Loader />
